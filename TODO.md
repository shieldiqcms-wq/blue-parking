<div dir="rtl">

# Blue Parking — التطويرات القادمة

هذا الملف يفصل بين **ما تم إنجازه فعلياً** و **ما لم يُنجز بعد**، بلا مبالغة.

---

## ✅ منجز ومُختبر

### قاعدة البيانات
- [x] 8 جداول: `profiles` · `vehicles` · `subscriptions` · `pricing_rules` · `parking_sessions` · `payments` · `ocr_captures` · `app_settings`
- [x] 3 عروض: `current_cars_inside` · `session_details` · `subscription_status`
- [x] محرك حساب الرسوم — مُختبر على 11 حالة
- [x] دوال الدخول/الخروج/التحصيل عبر RPC
- [x] دوال الإحصائيات والتقارير
- [x] RLS مفعّل على كل الجداول + مُختبر بحساب دخيل ودور مجهول
- [x] جداول المال للقراءة فقط من الواجهة
- [x] قيود تمنع: الدخول المزدوج، الخروج قبل الدخول، المبالغ السالبة، رسوماً على المشتركين
- [x] حفظ `pricing_rule_id` في كل عملية — الفواتير القديمة محصّنة
- [x] توحيد أرقام اللوحات (عربي/فارسي/لاتيني)

### الواجهة
- [x] 8 شاشات عربية RTL كاملة
- [x] تصميم Mobile-first + شريط سفلي للموبايل + قائمة جانبية للابتوب
- [x] نظام تصميم موحّد (أزرق/أبيض)
- [x] حالات تحميل وخطأ ونجاح في كل شاشة
- [x] كل رسائل الخطأ بالعربي عبر طبقة ترجمة واحدة
- [x] كشف انقطاع الإنترنت ومنع العمليات غير الآمنة
- [x] تصدير CSV يفتح في Excel بترميز عربي سليم

### OCR
- [x] `tesseract.js` مجاني يعمل داخل المتصفح
- [x] معالجة الصورة قبل القراءة
- [x] تأكيد يدوي إلزامي — لا اعتماد تلقائي
- [x] إدخال يدوي متاح دائماً
- [x] محرك قابل للاستبدال عبر واجهة `OcrEngine`
- [x] تسجيل كل محاولة في `ocr_captures`

### التشغيل والنشر
- [x] PWA: manifest + service worker + أيقونات
- [x] `lint` · `typecheck` · `build` — كلها نظيفة
- [x] GitHub Actions للنشر على Pages
- [x] `HashRouter` للتوافق مع GitHub Pages
- [x] ملف فحص سلامة `supabase/tests/verify.sql`

---

## ✅ مُختبر end-to-end

جُرّب التطبيق كاملاً على بيئة Supabase محلية بتسجيل دخول حقيقي عبر GoTrue:

- [x] تسجيل الدخول ولوحة التحكم
- [x] دخول سيارة بأرقام عربية (`١٢-٣٤٥٦٧` ← `1234567`)
- [x] إنشاء السيارة تلقائياً عند أول دخول
- [x] «السيارات الموجودة حالياً» مع المدة
- [x] معاينة المبلغ ← تسجيل الخروج والدفع
- [x] التقارير والتوزيع اليومي
- [x] الإعدادات مع أزرار أيام الإغلاق
- [x] السيارات والاشتراكات (حالات فارغة)

---

## ⚠️ لم يُختبر على أرض الواقع

هذه الأجزاء مكتوبة وتعمل تقنياً، لكنها **لم تُجرَّب في الموقف فعلياً**:

- [ ] **دقة قراءة اللوحة** على لوحات أردنية حقيقية بإضاءة الموقف
      — قد تحتاج ضبط `tessedit_pageseg_mode` أو معالجة صورة أفضل.
      الكاميرا لم تُجرَّب لأنها تحتاج جهازاً فيه كاميرا فعلية.
- [ ] **الكاميرا على جهاز أبو حمدان** — تحتاج HTTPS وإذن المتصفح
- [ ] **تثبيت PWA** على الجوال الفعلي
- [ ] **سرعة الاستخدام** في وقت الذروة (دخول عدة سيارات متتالية)

---

## 🔜 الأولوية العالية

- [ ] **تسجيل دفعات الاشتراكات الشهرية**
      حالياً `subscriptions.monthly_amount` للتوثيق فقط ولا يدخل في التقارير.
      المطلوب: ربط الاشتراك بجدول `payments` وإظهاره في إيرادات الشهر.
- [ ] **تعديل عملية مسجّلة بالخطأ**
      حالياً لا توجد طريقة لتصحيح وقت دخول خاطئ أو إلغاء عملية.
      المطلوب: دالة `amend_session` مع سجل تغييرات (audit log).
- [ ] **حفظ صورة اللوحة في Supabase Storage** (اختياري لكل عملية)
      الحقل `ocr_captures.image_path` موجود لكنه غير مستخدم بعد.
- [ ] **تنبيه انتهاء الاشتراكات** — يظهر العدد في الرئيسية لكن بلا قائمة مفصّلة.

---

## 🔹 الأولوية المتوسطة

- [ ] تقرير شهري قابل للطباعة (تنسيق طباعة محسّن)
- [ ] رسم بياني بسيط للإيرادات في التقارير
- [ ] سجل تغييرات التسعيرة (من غيّر، ومتى)
- [ ] بحث في العمليات برقم اللوحة داخل شاشة التقارير
- [ ] إظهار المبلغ التقديري للسيارات الموجودة حالياً (تحديث كل دقيقة)
- [ ] `plate_normalized` للسيارات: إضافة فهرس `pg_trgm` لتسريع البحث الجزئي
      إذا تجاوز عدد السيارات بضعة آلاف

---

## 🔸 أفكار مستقبلية (خارج نطاق الـ MVP الحالي)

- [ ] أكثر من مستخدم (موظفون بصلاحيات محدودة) — البنية تدعمه عبر `profiles.role`
- [ ] أكثر من موقف
- [ ] بوابة دخول/خروج آلية
- [ ] إشعارات WhatsApp / SMS للمشتركين قبل انتهاء الاشتراك
- [ ] بوابة دفع إلكتروني
- [ ] تطبيق أندرويد/آيفون من المتجر (حالياً PWA)
- [ ] محرك OCR أدق (نموذج مخصّص للوحات الأردنية)

---

## 🧱 دين تقني معروف

- [ ] `normalizePlate` مكتوبة مرتين (TypeScript + SQL) — **عند تعديل إحداهما
      عدّل الأخرى**. `supabase/tests/verify.sql` يتحقق من نسخة SQL.
- [ ] لا توجد اختبارات آلية للواجهة (Vitest / Playwright) — الاختبار الحالي
      لقاعدة البيانات فقط.
- [ ] `session_details` تُحمّل حتى 500 صف بلا ترقيم صفحات — كافٍ لسنوات في موقف
      صغير، لكن يحتاج ترقيماً لو كبر الحجم.
- [ ] الأسبوع يبدأ يوم الأحد (مثبَّت في `get_dashboard`) — لو أردت السبت، عدّل
      حساب `v_week_start`.
- [ ] `closed_days` تنبيه فقط ولا يمنع التسجيل — مقصود، لكن لو أردت منعاً
      صارماً فالمكان هو `register_entry` في migration الدوال.

---

## ✔️ قرارات حُسمت من أبو حمدان

| السؤال | القرار | التطبيق |
|---|---|---|
| المبيت في الموقف | لا يوجد مبيت — المبلغ الأساسي لمن دخل بين 8 و 3 | مطبّق |
| الدخول قبل 08:00 | **يُحاسب** | `charge_before_start = true` |
| الجمعة والسبت | **لا يوجد دوام** | `closed_days = {5,6}` + تنبيه في الواجهة |
| فترة السماح | **15 دقيقة** بعد الثالثة | `grace_minutes = 15` |

كلها مُختبرة على PostgreSQL 15 (15 حالة حساب) وقابلة للتعديل من شاشة الإعدادات.

---

## ⏳ قرارات ما زالت مفتوحة

- [ ] **العطل الرسمية** (عيد، استقلال…): حالياً تُعامل كأيام عمل عادية.
      `closed_days` يغطي أيام الأسبوع فقط، لا تواريخ محددة.
      لو احتجناها: جدول `holidays` بتواريخ + مرجع في `is_closed_day`.
- [ ] **سيارة تنسى في الموقف ليوم أو أكثر**: حالياً تُحاسب على كل الساعات
      (يوم كامل ≈ 19 د.أ). القاعدة صحيحة تقنياً، لكن قد تحتاج سقفاً أعلى
      للمبلغ اليومي إذا صارت الحالة تتكرر.
- [ ] **تسعيرة موسمية**: البنية تدعم أكثمن قاعدة تسعير، لكن الفعّال حالياً
      قاعدة واحدة. لو احتجنا تسعيرة رمضان أو الصيف، نحتاج ربط القاعدة بمدى
      تاريخي (`valid_from` / `valid_to`).

</div>
